<%= form_for(@public_photo) do |f| %>
  <% if @public_photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@public_photo.errors.count, "error") %> prohibited this public_photo from being saved:</h2>

      <ul>
      <% @public_photo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :image %><br>
    <%= f.file_field :image %>
  </div>
  <div class="field">
    <%= f.label :caption %><br>
    <%= f.text_field :caption %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
